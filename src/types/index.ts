export type TXY = {
  x: number;
  y: number;
};

export type TXYWH = {
  x: number;
  y: number;
  w: number;
  h: number;
};

export type TKeys = {
  a: {
    pressed: boolean;
  };
  d: {
    pressed: boolean;
  };
  k: {
    pressed: boolean;
  };
  j: {
    pressed: boolean;
  };
};
